import { NavComponent } from "./Navigation";

const components: NavComponent[] = [
  {
    id: "new-product",
    type: "button",
    title: "New Product",
    href: "/admin/product",
  },
];

export default components;
