import { NavComponent } from "./Navigation";

const components: NavComponent[] = [
  {
    id: "products",
    type: "button",
    title: "New Product",
    href: "/admin/products",
  },
];

export default components;
