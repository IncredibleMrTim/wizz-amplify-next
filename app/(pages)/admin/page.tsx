const AdminPage = () => {
  // Do this so when the page loads, it will check if the user is authenticated
  // and update the state in the redux store

  return (
    <div>
      <h1>Admin Page</h1>
      <p>This is the admin page.</p>
    </div>
  );
};
export default AdminPage;
